# Password Generator

A minimal React password generator built as a static site.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

The project uses `base: '/'` in `vite.config.js`, which fits GitHub Pages with a custom domain and Cloudflare in front of it.
